<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="shortcut icon" type="png" href="/image/head_logo.png">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <title>Seychelles Bank (Pvt) Ltd.</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">Account Details</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <a class="btn btn-secondary my-2 my-sm-0" href="login.html" role="button">LOG OUT</a>
                </ul>
            </div>
        </nav>
    </header>
    {{#if error}}
        <div class="row alert alert-danger m-3" role="alert">
            {{error}}
        </div>
    {{/if}}
    {{#if success}}
        <div class="row alert alert-success m-3" role="alert">
            {{success}}
        </div>
    {{/if}}
    <main>
        <div class="card text-center border-dark">
            <div class="card-header bg-secondary" style="color: white;">Account Description</div>
            <div class="card-body row">
                <p class="col">{{ account.acc_id }}</p>
                <p class="col">{{ branch.branch_name }} Branch</p>
                <p class="col">{{ account.acc_type }}</p>
                <p class="col-5">{{ account.created_date }}</p>
            </div>
            {{#if deposits}}
                <div class="card-header bg-secondary" style="color: white;">Deposits</div>
                <table class="table table-bordered mt-3">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">Transaction ID</th>
                            <th scope="col">Account ID</th>
                            <th scope="col">Type</th>
                            <th scope="col">Amount</th>
                            <th scope="col">Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each deposits}}
                            <tr>
                                <td>{{this.trans_id}}</td>
                                <td>{{this.acc_id}}</td>
                                <td>{{this.deposit_type}}</td>
                                <td>{{this.amount}}</td>
                                <td>{{this.date}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{/if}}
            {{#if withdrawals}}
                <div class="card-header bg-secondary" style="color: white;">Withdrawals</div>
                <table class="table table-bordered mt-3">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">Transaction ID</th>
                            <th scope="col">Account ID</th>
                            <th scope="col">Type</th>
                            <th scope="col">Amount</th>
                            <th scope="col">Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each withdrawals}}
                            <tr>
                                <td>{{this.trans_id}}</td>
                                <td>{{this.acc_id}}</td>
                                <td>{{this.withdraw_type}}</td>
                                <td>{{this.amount}}</td>
                                <td>{{this.date}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{/if}}
            {{#if transfers}}
                <div class="card-header bg-secondary" style="color: white;">Transfers</div>
                <table class="table table-bordered mt-3">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">Transaction ID</th>
                            <th scope="col">From Account ID</th>
                            <th scope="col">To Account ID</th>
                            <th scope="col">Amount</th>
                            <th scope="col">Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each transfers}}
                            <tr>
                                <td>{{this.trans_id}}</td>
                                <td>{{this.from_acc_id}}</td>
                                <td>{{this.to_acc_id}}</td>
                                <td>{{this.amount}}</td>
                                <td>{{this.date}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{/if}}
        </div>
        <a href="/Customer/{{user.user_id}}" type="button" class="btn btn-info btn-block mt-3">Dashboard</a>
    </main>
    <footer class="bottom">Seychells Bank (Pvt) Ltd. - Version 1.0.0 © 2020 | <a href="">Visit Us</a></footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>