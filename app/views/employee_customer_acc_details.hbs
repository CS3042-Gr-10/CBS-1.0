<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="shortcut icon" type="png" href="/image/head_logo.png">
    <link rel="stylesheet" href="/stylesheets/nav_main_footer.css">
    <title>Seychelles Bank (Pvt) Ltd.</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">Individual Account</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <a class="btn btn-success my-2 my-sm-0 mr-4" href="/employee/{{user.user_id}}/customerTransaction" role="button">Make a Transaction</a>
                    <a class="btn btn-secondary my-2 my-sm-0" href="/logout" role="button">LOG OUT</a>
                </ul>
            </div>
        </nav>
    </header>
    {{#if error}}
        <div class="row alert alert-danger my-3" role="alert">
            {{error}}
        </div>
    {{/if}}
    {{#if success}}
        <div class="row alert alert-success my-3" role="alert">
            {{success}}
        </div>
    {{/if}}
    <main>
        <div class="card text-center border-dark">
            <div class="card-header bg-secondary" style="color: white;">Account Details</div>
            <div class="card-body row">
                <p class="col">{{ full_name }}</p>
                <p class="col">{{ acc_number }}</p>
                <p class="col">{{ NIC_number }}</p>
                <p class="col">{{ acc_type }}</p>
                <p class="col-5">{{ open_date }}</p>
            </div>
            <div class="card-header bg-secondary" style="color: white;">Recent Withdrawals</div>
            <table class="table table-bordered table-striped mt-3">
                <thead class="table-primary">
                    <tr>
                        <th>Transaction ID</th>
                        <th>Withdraw Type</th>
                        <th>Amount</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each withdrawals}}
                        <tr>
                            <td>{{this.trans_id}}</td>
                            <td>{{this.withdraw_type}}</td>
                            <td>{{this.amount}}</td>
                            <td>{{this.date}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
            <div class="card-header bg-secondary" style="color: white;">Recent Deposits</div>
            <table class="table table-bordered table-striped mt-3">
                <thead class="table-primary">
                    <tr>
                        <th>Transaction ID</th>
                        <th>Deposit Type</th>
                        <th>Amount</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each deposits}}
                        <tr>
                            <td>{{this.trans_id}}</td>
                            <td>{{this.deposit_type}}</td>
                            <td>{{this.amount}}</td>
                            <td>{{this.date}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        <a href="/employee/{{User.user_id}}" type="button" class="btn btn-info btn-block mt-3">Back</a>
    </main>
    <footer class="bottom">Seychells Bank (Pvt) Ltd. - Version 1.0.0 © 2020 | <a href="">Visit Us</a></footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>