<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="shortcut icon" type="png" href="/image/head_logo.png">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <link rel="stylesheet" href="/stylesheets/dycalendar.min.css">
    <title>Seychelles Bank (Pvt) Ltd.</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">FD Details</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <a class="btn btn-secondary my-2 my-sm-0" href="/logout" role="button">LOG OUT</a>
                </ul>
            </div>
        </nav>
    </header>
    {{#if error}}
        <div class="row alert alert-danger m-3" role="alert">
            {{error}}
        </div>
    {{/if}}
    {{#if success}}
        <div class="row alert alert-success m-3" role="alert">
            {{success}}
        </div>
    {{/if}}
    <main>
        <div class="card shadow border-success rounded" style="margin: 20px auto; max-width: 375px;">
            <div class="card-body col px-4" style="text-align: left;">
                <p><span style="font-weight: bold;">FD ID</span> : {{ FD.fd_id }}</p>
                <p><span style="font-weight: bold;">Customer ID</span> : {{ FD.customer_id }}</p>
                <p><span style="font-weight: bold;">Related Savings Account</span> : {{ FD.sv_acc_id }}</p>
                <p><span style="font-weight: bold;">Branch</span> : {{branch.branch_name}}</p>
                <p><span style="font-weight: bold;">Balance</span> : {{FD.balance}}</p>
                <p><span style="font-weight: bold;">State</span> : {{FD.state}}</p>
                <p><span style="font-weight: bold;">Plan</span> : {{FD.name}}</p>
                <p><span style="font-weight: bold;">Duration</span> : {{FD.duration}}</p>
                <p><span style="font-weight: bold;">Interest</span> : {{FD.interest_rate_per_mon}}</p>
                <p><span style="font-weight: bold;">Opening Date</span> : {{ FD.opened_date }}</p>
            </div>
        </div>
        <a href="/Customer/{{user.user_id}}/fds" type="button" class="btn btn-info btn-block" style="margin: 20px auto; max-width: 375px;">Back</a>
    </main>
    <footer class="fixed-bottom">Seychells Bank (Pvt) Ltd. - Version 1.0.0 © 2020 | <a href="">Visit Us</a></footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>