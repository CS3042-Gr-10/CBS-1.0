<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="shortcut icon" type="png" href="/image/head_logo.png">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <title>Seychelles Bank (Pvt) Ltd.</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">My Loans</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <a class="btn btn-secondary my-2 my-sm-0" href="/logout" role="button">LOG OUT</a>
                </ul>
            </div>
        </nav>
    </header>
    {{#if error}}
        <div class="alert alert-danger text-center m-3" role="alert">
            {{error}}
        </div>
    {{/if}}
    {{#if success}}
        <div class="alert alert-success text-center m-3" role="alert">
            {{success}}
        </div>
    {{/if}}
    <main>
        <div class="card text-center border-dark" style="margin: auto; max-width: 1000px;">
            <div class="card-header bg-secondary" style="color: white;">Approved Loans</div>
            <div class="card-body">
                <div class="card border-success rounded mb-3">
                    <div class="card-body">
                        <div class="row" style="text-align: center; justify-content: center; font-weight: bold;">
                            <p class="col">Loan ID</p>
                            <p class="col">Loan Type</p>
                            <p class="col">Amount</p>
                            <p class="col">No. of Installments Paid</p>
                        </div>
                    </div>
                </div>
                {{#each Loans}}
                    <div class="card shadow border-success rounded mb-3">
                        <a class="card-block stretched-link text-decoration-none" href="{{this.url}}"></a>
                        <div class="card-body">
                            <div class="row" style="text-align: center; justify-content: center;">
                                <p class="col">{{this.loan_id}}</p>
                                <p class="col text-secondary">{{this.loan_type}}</p>
                                <p class="col text-secondary">{{this.loaned_amount}}</p>
                                <p class="col text-secondary">{{this.finished_num_installements}}</p>
                            </div>
                        </div>
                    </div>
            {{/each}}
            </div>
        </div>
        <a href="/customer/{{user.user_id}}" type="button" class="btn btn-info btn-block mt-3" style="margin: auto; max-width: 1000px;">Back</a>
    </main>
    <footer class="fixed-bottom">Seychells Bank (Pvt) Ltd. - Version 1.0.0 © 2020 | <a href="">Visit Us</a></footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>