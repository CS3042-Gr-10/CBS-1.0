<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="shortcut icon" type="png" href="/image/head_logo.png">
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <title>Seychelles Bank (Pvt) Ltd.</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">User Profile</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <a class="btn btn-secondary my-2 my-sm-0" href="/logout" role="button">LOG OUT</a>
                </ul>
            </div>
        </nav>
    </header>
    {{#if error}}
        <div class="alert alert-danger text-center m-3" role="alert">
            {{error}}
        </div>
    {{/if}}
    {{#if success}}
        <div class="alert alert-success text-center m-3" role="alert">
            {{success}}
        </div>
    {{/if}}
    <main>
        <div class="card text-center border-dark">
            <div class="card-header bg-secondary" style="color: white;">User Details</div>
            <div class="card-body">
                <h5 class="card-title">{{ customer.first_name }} {{ customer.last_name }}</h5>
                <div class="row" style="text-align: left; justify-content: center;">
                    <div class="col-2"></div>
                    <div class="col-3">
                        <p>Email : {{customer.email}}</p>
                        <p>Contact No.(1) : {{customer.contact_primary}}</p>
                        <p>Gender : {{ customer.gender }}</p>
                        <p>Postal code : {{customer.postal_code}}</p>
                    </div>
                    <div class="col-5">
                        <p>NIC : {{ customer.NIC}}</p>
                        <p>Contact No.(2) : {{customer.contact_secondary}}</p>
                        <p>DOB : {{ customer.dob }} </p>
                        <p>Address : {{ customer.house_no }}, {{customer.street}}, {{customer.city}}</p>
                    </div>
                </div>
            </div>
            <div class="card-header bg-secondary" style="color: white;">Related Accounts</div>
            <div class="card-body">
                {{#each accounts}}
                <div class="card shadow border-success rounded mb-2">
                    <a class="card-block stretched-link text-decoration-none" href="{{this.url}}"></a>
                    <div class="card-body">
                        <div class="row" style="text-align: center; justify-content: center;">
                            <p class="col">{{this.acc_id}}</p>
                            <p class="col text-secondary"> {{this.acc_type}} </p>
                            <p class="col-5 text-secondary">{{this.created_date}}</p>
                        </div>
                    </div>
                </div>
            {{/each}}
            </div>
        </div>
        <a href="/customer/{{user.user_id}}" type="button" class="btn btn-info btn-block mt-3">Back</a>
    </main>
    <footer class="fixed-bottom">Seychells Bank (Pvt) Ltd. - Version 1.0.0 © 2020 | <a href="">Visit Us</a></footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
